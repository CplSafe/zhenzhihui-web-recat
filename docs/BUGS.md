# 帧智汇试用反馈 / Bug 清单

梳理日期：2026-09-22。上一版是 2026-09-20 的《帧智汇试用反馈处理清单（汇总版）》（Claude Doc），本版在其基础上按 9/20–9/22 的代码进展更新状态，并把「未解决」按 **责任端** 重新分组，方便直接派单。

## 一、总览

| 状态 | 数量 | 说明 |
| --- | --- | --- |
| 已解决 | 13 | 已合入 dev 且提出人无异议，或线上已恢复并在群里答复 |
| 已修复·待验证 | 7 | 代码已全部在 dev（9/22 上午合入），**待部署后让提出人回归**（见第二节） |
| 未解决 | 12 | 需要后端 / 运维 / 产品先动，或信息不足待复现 |
| 合计 | 32 | 来源：《使用反馈.docx》11 条（无限画布）+ 企微群 12 条 + 私聊截图 9 条 |

状态口径与上一版一致：已解决 = 合入 dev 或线上答复；已修复·待验证 = 修了但没回归；未解决 = 没修 / 只做了解释 / 被后端或供应商阻塞。

### 9/20 以来的变化

- 9/20：`fe-feedback-fixes` 分支（`2e4f068`）收掉 U8、U10、U13 全部，以及 U1、U2、U3 的前端部分。
- 9/22 上午（本次）：
  - U17 修复提交到 dev（`a6ba7a4`）。
  - `fe-feedback-fixes` 合入 dev（`71af04f`）。合并时与工作区里未提交的「分段画面修改」WIP（VideoStage 五秒分段编辑器）冲突两处，已解决：`TaskCenterDrawer` 取两边 import 并集；`VideoStage` 保留分段编辑器，「整段视频修改」下方的能力说明改为只在可分段时显示，并把用户引到上方分段编辑器（该处改动随 WIP 留在工作区，未单独提交）。
  - U16 加固提交到 dev（`67738ae`）：背景视频卸载时 `removeAttribute('src') + load()` 彻底关掉媒体管线；有声自动播放被拦截后，后续 canplay 不再自动恢复声音。内置浏览器实测登录页 → 开屏页 → 首页，离开后旧 `<video>` 均为 paused / muted / `networkState 0`。
- 这批改动 dev 上 `npm run typecheck` 通过、vitest 3110 用例通过（另一条失败是 WIP 测试与合并文案重叠，已随冲突处理消除）。

### 本周动作（按顺序）

1. ~~提交 U17、合入 `fe-feedback-fixes`~~（已做）→ **部署 dev**。
2. 部署后在群里 @ 提出人回归：G1、G4、U8、U10、U13、U16、U17。
3. 运维先确认火山（U14）和 Google 图片计费（U15）两个账号——不恢复的话 G1 / U3 / 图生图的回归结论都不可信。
4. 后端：U2 拉供应商失败详情，U3 查 `/assets/{id}/complete` 的 4xx。
5. 产品：U1 与客户（Petra Pan / LTY）确认「3–5 s 分段生成 + 单段重做」方案。

## 二、已修复·待验证（7 条）

全部已在 dev，下一步是部署后让提出人回归。

| 编号 | 问题 | 修复 | dev 提交 | 还差什么 |
| --- | --- | --- | --- | --- |
| G1 | 爆款成片「整段视频修改」报「参考素材不可用」（LTY 9/10） | 「确认修改」改走 `video.edit` | `fdcfd28` | 提出人回归；后续一次复发疑与火山欠费同源（U14） |
| G4 | 画布视频节点供应商失败后一直「查询失败自动重试」（LTY 9/16） | 供应商服务级失败落终态 + 友好文案 | `40d206b` | 提出人回归 |
| U8 | 图片超出 256–5760 px 才在生成时报英文错误（2046 9/10） | 选图时预校验像素 256–5760 与宽高比 0.4–2.5，>5760 自动缩放，中文提示；覆盖智能成片 / 图片对话 / 爆款复制 / 画布 / 真人素材 | `2e4f068`（经 `71af04f` 合入） | 部署、回归 |
| U10 | 全能参考模式视频最多 13 s，14 / 15 s 灰色无说明（LTY 9/16） | 时长档位下标「当前模型可选 X–Y 秒」，不可选档位悬停给原因 | `2e4f068`（经 `71af04f` 合入） | 部署、回归 |
| U13 | 错误提示直接暴露供应商原文（metaso 402、content[1].image_url、provider task failed） | `business.ts` 统一翻译层；任务中心通知 / 抽屉、历史卡片、画布、爆款成片持久化错误全部接入 | `2e4f068`（经 `71af04f` 合入） | 部署、回归 |
| U16 | 在其他页面能听见登录页背景视频声音；显示静音仍有声（杜浩然 9/17） | 在 `c76dbd4`（离页停音 + muted 同步）之上：卸载时卸掉 `src` 并 `load()` 彻底关掉媒体管线；有声自动播放被拦截后不再自动恢复声音，只有点喇叭才恢复 | `c76dbd4`、`67738ae` | 部署后复验；仍在则用 DevTools `document.querySelectorAll('video,audio')` 定位 |
| U17 | 「我的素材」对图片点「去创作」后入口缩略图裂图（杜浩然 9/20；原 BUG-001） | 跳转只传 `assetId`，入口按 `assetId` + 工作空间生成同源下载地址，不再透传短期签名 URL | `a6ba7a4` | 部署、回归 |

## 三、未解决（12 条）

优先级：P0 = 阻断核心流程或多位用户反复反馈；P1 = 影响体验 / 交付质量，有绕行办法；P2 = 增强项或信息不足待复现。

### 3.1 需要后端 / 产品先动（9 条）

| 优先级 | 编号 | 问题 | 来源 | 现状 / 原因 | 需要谁做什么 |
| --- | --- | --- | --- | --- | --- |
| P0 | U1 | 爆款成片「片段修改」只改几秒从未成功，整段修改也不能只改局部，每次只能回脚本整体重生成 | LTY 9/11、9/15、9/16；杜浩然 9/15 | `video.edit` / `video.generate` 只吃「源视频 + 一段文字」，模型没有按秒区间重绘能力；片段修改入口已下线。前端文案部分已做（`2e4f068`：说明「整段重生成、不支持只改几秒、建议 3–5 秒分镜单独重做」） | **产品**定方案（分段生成 + 单段重做，客户 Petra Pan 建议 3–4 s 一段）；**后端**另立项做分段生成再拼接 |
| P0 | U2 | 画布视频节点（Framora 1.0，全能参考，5 张图，9:16，10–13 s）生成失败 `provider task failed with status failed`，AI 润色多次仍失败 | LTY 9/16 | 供应商侧任务失败，原因未定位。前端已把原文翻成中文（`2e4f068`） | **后端**拉供应商失败详情，确认参考图数量 / 尺寸 / 时长组合是否超限 |
| P0 | U3 | 画布上传图片后提示「素材文件已上传，但素材状态确认失败」 | LTY 9/16；杜浩然截图 9/16（线上） | 文件已到存储，但 `POST /assets/{id}/complete` 返回 4xx；怀疑火山欠费导致入库元数据处理失败，真实响应码未拿到。前端已在提示里带上 HTTP 状态码与后端 message（`2e4f068`） | **后端**查 `/complete` 的 4xx；部署前端后群里一张截图即可分辨 |
| P1 | U4 | 改台词后重新生成配音声音变了，客户要求同一角色声音一致，希望有「AI 声音」选择 | LTY 9/15 | TTS 每次音色不固定；已答复「单独功能，需要时间」 | **后端**音色固定 + 可选音色列表；前端加选择器。与 U5 合并排期，短剧客户刚需，建议作下个迭代主线 |
| P1 | U5 | 不支持导入音频文件，短剧无法保持人物对话音色一致 | 《使用反馈》#11 | 需后端音频资产 + 生成链路支持 | 先做 U4，再做音频导入 / 音色克隆 |
| P1 | U6 | 「历史生成」记录不能删除；右侧历史列表每次刷新都要等 | 苗橙 9/10 | 历史版本存在项目草稿，前端从本地 / 后端草稿多源合并，单删本地会被后端合回来；列表慢是每张缩略图都在拉视频元数据 | **后端**先定「隐藏标记」的持久化口径（草稿字段或接口）；前端再加删除按钮、缩略图改用封面图 |
| P1 | U7 | 用 SD 视频模型时没有「资产审核」按钮 | 《使用反馈》#2 | 测试后端 199 个 swagger 接口里没有素材合规审核接口 | **后端**先提供「提交素材 → 返回合规结果」接口 |
| P1 | U9 | seedream 5.0 选的比例没真正传给模型；模型是 pro 还是 lite 未标注 | 《使用反馈》#9 后半 | 画布只在模型 `params_schema` 声明了比例字段时才下发，否则只改节点画幅 | **后端**在 `params_schema` 声明比例字段（`aspect_ratio` / `size` 定一个）并在模型列表标注版本；前端再接入 |
| P2 | U11 | 希望在 MJ、banana 之外增加 GPT 生图模型 | 《使用反馈》#10 | 后端模型接入 | **后端**评估成本后接入 GPT Image，前端模型目录自动出现 |

### 3.2 需要运维先确认账号（2 条）

| 优先级 | 编号 | 问题 | 来源 | 现状 / 原因 | 需要谁做什么 |
| --- | --- | --- | --- | --- | --- |
| P1 | U14 | 爆款复刻报 volcengine 404 `InvalidEndpointOrModel.NotFound`，随后 403 `AccountOverdueError` | 杜浩然截图 9/11 | 火山接入点不存在 / 账号欠费，后端原样透传；前端已加中文兜底。G1、U3 的复发怀疑同源；账号是否恢复未确认 | **运维**确认 zzh 部署用的火山账号余额与接入点配置，确认后回归 G1 / U3 |
| P1 | U15 | Nano Banana 2 图生图报 502 `PROVIDER_FAILED`（Google image billing usage is missing） | 杜浩然截图 9/17 | Google 图片接口计费未配置；前端已落终态并提示「换模型 / 联系管理员」 | **运维 / 后端**配好 Google 计费后回归 |

### 3.3 前端待复现（1 条）

| 优先级 | 编号 | 问题 | 来源 | 现状 / 原因 | 下一步 |
| --- | --- | --- | --- | --- | --- |
| P2 | U12 | 画布生成时页面疑似卡死（用户 7 秒录屏「直接卡死在这里」） | LTY 9/16 | 信息不足，未复现；录屏分不清是前端卡死还是生成等待 | 向用户要浏览器 / 机型与复现步骤，复现后再定改动 |

前端这边还能独立推进但本次没做的：U6 的「历史列表慢」——每个历史版本缩略图都是 `<video preload="metadata" src=…#t=0.1>`，逐个拉元数据；成片版本记录里没有封面资产，改封面图前得先让生成链路产出封面（后端或前端截帧上传），属于 U6 的口径问题。

## 四、已解决（13 条）

### 4.1 无限画布《使用反馈.docx》（8 条，9/17–9/18 已推 dev）

| 编号 | 反馈 | 处理 | 提交 |
| --- | --- | --- | --- |
| #1 | 多节点不能鼠标框选，只能 Ctrl 逐个点 | 工具栏「框选」开关；按住 Shift 拖拽也可框选 | `753db33` |
| #3 | 节点不能用 Del 删除 | Delete / Backspace 均可删除选中节点 | `605b651` |
| #4 | 文本 / 图片 / 视频节点不能复制、创建副本 | 右键「生成副本」+ Ctrl/Cmd+D，副本保留提示词与参数、不带运行态 | `605b651` |
| #5 | 视频节点提示词框不能放大 | 「放大 / 收起」+ 拖拽调高 | `605b651` |
| #6 | 提示词里不能直接 @ 图片参考 | 输入 @ 弹参考选择器（缩略图 + 标签，可筛选）；重命名 / 删除后引用自动同步；补了取图入口与叠层对齐 | `52606ff`、`6dfcb28`、`794fc7f` |
| #7 | 没有自定义截帧 | 放大预览里拖进度条任意位置「截取此帧」；截帧菜单加「自定义截帧…」 | `042b696`、`794fc7f` |
| #8 | 同一节点看不到自己的生成历史 | 节点面板缩略图历史条（最多 12 条），可回退旧版本 | `61593cf` |
| #9 | seedream 5.0 不能选生图比例 | 比例选择器已放开；比例真正下发给模型的部分拆为 U9 | `11828aa`（部分） |

《使用反馈》剩下的 #2、#10、#11 需后端先动，对应 U7、U11、U5。

### 4.2 企微群与截图反馈（5 条）

| 编号 | 反馈 | 处理 | 提交 |
| --- | --- | --- | --- |
| G2 | 视频生成失败 metaso HTTP 402 `insufficient_balance_error`（LTY 9/15） | 供应商余额不足，当天充值恢复 | —（运维） |
| G3 | 视频生成失败 `media dimensions must be between 256 and 5760 pixels`（2046 9/10） | 已告知限制；前端预校验拆为 U8 | —（解释） |
| G5 | 画布图生图所有模型都报「素材类型不适用于当前操作」（杜浩然 9/17） | 图片编辑参考素材角色按供应商声明下发，不再一律 `reference_image`；错误文案先引导换素材。剩余 Google 计费 502 拆为 U15 | `0d31a89`、`d9a0855` |
| G6 | 上传 1:1 图片后画布视频只能出 1:1（杜浩然 9/10） | 视频节点默认全能参考 + 16:9，参考图不再锁死输出比例 | `7ba8998` |
| G7 | 图片同步生成等 2 分钟就超时（杜浩然 9/16） | 同步生图超时 2 → 10 分钟 | `a00b8dc` |

## 五、来源与提出人

| 来源 | 说明 | 提出人 |
| --- | --- | --- |
| 《使用反馈.docx》 | 对标 Neowow 的无限画布试用反馈，11 条（9 月，Clarence 转发） | 试用用户（短剧制作方向） |
| 企微「帧尚AI内容群-3」 | 9/10–9/19 群聊，含截图 / 录屏，12 个问题点 | LTY、2046、苗橙、Petra Pan |
| 企微私聊截图 / 录屏 + 自测 | 9/9–9/20 转到开发会话分析的线上报错截图 | 杜浩然 |

上一版：Claude Doc《帧智汇试用反馈处理清单（汇总版）》（2026-09-20，<https://claude.ai/artifact/S7VQzTp71wpHMEEzw1vaTr>）；更早一版在 `D:\门亚旭-简历\帧智汇使用介绍\帧智汇试用反馈处理清单_2026-09-20.docx`。
